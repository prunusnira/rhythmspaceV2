using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace BMSPlayer
{
    public enum NoteSkin
    {
        NORMAL = 0,
        PASTEL = 1
    }

    public enum NoteSize
    {
        NORMAL = 0,
        SLIM = 1,
        FAT = 2
    }
}
